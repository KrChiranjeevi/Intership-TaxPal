{
  "framework": "angular",
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "dist/client/browser",
  "installCommand": "npm install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
